"""Continuously Augmented Discrete Diffusion (CADD).

This repository primarily targets discrete-token image generation. CADD combines
an absorbing discrete masking process with a coupled continuous latent diffusion
process in embedding space.
"""

from .cadd_model import CADD
