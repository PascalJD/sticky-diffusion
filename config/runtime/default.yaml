# config/runtime/default.yaml
device: auto  # auto | cpu | gpu
platform: auto  # auto | single | pmap  

# XLA memory behavior (GPU mainly)
xla_preallocate: false
xla_mem_fraction: 0.85  # 0..1 or null to omit

pmap_axis_name: batch
