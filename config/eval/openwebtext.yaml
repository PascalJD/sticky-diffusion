# Lightweight OpenWebText training-time evaluation.
enabled: true
mode: text_basic
prefix: eval
log_at_step_zero: false
run_at_end: true

fid_enabled: false
is_enabled: false

text_every: 10000
text_num_samples: 16
text_batch_size: 16
text_output_prefix: text_samples
