train:
  epochs: 100
  batch_size: 256
  lr: 1e-4
  eps: 1e-3
  M_events: 100000
  

sampling:
  steps: 50
  eps: 1e-4

logging:
  project: sticky-diffusion
  name: mnist-sticky
  sample_every: 500
  n_samples: 16
  log_every: 50