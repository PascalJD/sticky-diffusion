# Uniform-transition D3PM experiment bundle for CIFAR-10.
defaults:
  - /dataset: cifar10_discrete
  - /model/baseline@model: d3pm_uniform_cifar10
  - /optim: adamw_cifar10
  - /sampler: d3pm_cifar10
  - /training: cifar10
  - /runtime: default
  - _self_

task:
  name: d3pm_uniform_cifar10
