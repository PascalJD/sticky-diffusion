# Canonical CIFAR-10 experiment bundle for Bit Diffusion.
defaults:
  - /dataset: cifar10_discrete
  - /model/baseline@model: bitdiff_cifar10
  - /optim: adamw_cifar10
  - /sampler/cifar10@sampler: bitdiff_cifar10
  - /training: cifar10
  - /runtime: default
  - /eval@_global_.eval: cifar10
  - _self_

task:
  name: bitdiff_cifar10
