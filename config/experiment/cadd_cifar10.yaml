# Canonical CIFAR-10 experiment bundle for CADD.
defaults:
  - /dataset: cifar10_discrete
  - /model: cadd_cifar10
  - /optim: adamw_cifar10
  - /training: cifar10
  - /runtime: default
  - _self_

task:
  name: cadd_cifar10
