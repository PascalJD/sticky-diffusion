# @package experiment
defaults:
  - sjd_anchor_study_cifar10
  - override /model/anchor@model.anchor: ordered_scalar
  - _self_

task:
  name: sjd_anchor_study_cifar10_fixed_ordered_scalar

model:
  anchor:
    learnable: false
