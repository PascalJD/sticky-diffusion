# @package experiment
defaults:
  - sjd_anchor_study_cifar10
  - override /model/anchor@model.anchor: normal_64
  - _self_

task:
  name: sjd_anchor_study_cifar10_fixed_normal_64

model:
  anchor:
    learnable: false
