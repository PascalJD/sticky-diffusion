# Canonical CIFAR-10 experiment bundle for the continuous DDPM baseline.

defaults:
  - /dataset: cifar10_discrete
  - /model: ddpm_cifar10
  - /optim: adamw_cifar10
  - /sampler: ddpm_cifar10
  - /training: cifar10
  - /runtime: default
  - _self_

task:
  name: ddpm_cifar10
