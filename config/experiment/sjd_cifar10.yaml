# SJD experiment bundle for CIFAR-10.

defaults:
  # Core components.
  - /dataset: cifar10_discrete
  - /model: sjd_cifar10
  - /optim: adamw_md4
  - /training: sjd_cifar10
  - /runtime: default

  # SJD-specific sampling and forward-process pieces.
  - /sampler: default
  - /forward/beta: vp_linear
  - /forward/hazard: early
  - /forward/jump: vp_matched

  - _self_

task:
  name: sjd_cifar10
