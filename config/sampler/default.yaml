# config/sampler/default.yaml
T: ${oc.select:forward.beta.T, 1.0}
n_steps: 250

alloc_mode: argmax
score_from_classifier: true  # Only implemented so far
score_scale: 1.0

hazard_mode: plugin
log_ratio_clip: 10.0
intensity_chunk_size: 256

init_std: 1.0
eps_denom: 1e-12
rng_fold: 4

force_classify_at_end: true
