# config/training/sjd_cifar10.yaml

name: sjd_cifar10

seed: 0

num_train_steps: 2000000

ema_rate: 0.9999

log_every_steps: 100
eval_every_steps: 5000
checkpoint_every_steps: 5000
log_images_every_steps: 2500
num_log_images: 16
sample_timesteps: ${..sampler.n_steps}