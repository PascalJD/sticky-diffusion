# Weights & Biases logging defaults.

# Global switch.
enabled: true

# Run metadata.
project: sticky-diffusion
entity: null
run_name: null
tags: []

# Logging backend:
#   online   -> sync immediately
#   offline  -> store locally for later sync
#   disabled -> no-op even if enabled=true
mode: online
